//var connections = require('./connection_data.json');
//var nodes = require('./node_data.json');

//for (var i = 0; i < nodes.length; i++) {
    //var connectingNodes = connections[nodes[i].id];
    //nodes[i].connections = connectingNodes;
//}

//console.log(JSON.stringify(nodes));

// comment out this code if not using it...
// Unity's JS compiler complains about several bits in here
